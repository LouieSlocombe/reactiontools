# User guide

The how-to half of the documentation: one page per thing you are likely to be
doing, in roughly the order a project runs into them. Each assumes the
[quickstart](../quickstart.md) has been read; none assumes the others have.

For the signature and every argument of any function named here, see the
[API reference](../api/index.md).

```{toctree}
:maxdepth: 2

neb
restarting
parallel
transition-states
end-states
metadynamics
plumed-postprocessing
units
```
